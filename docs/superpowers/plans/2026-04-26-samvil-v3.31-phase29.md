# SAMVIL v3.31 Phase 29 — Evolve Cycle Closure

## Goal

Phase 28 routes rebuilt evolved output back to QA. Phase 29 records whether the
cycle is actually closed, needs another evolve pass, or must stop for failure,
using only project-local artifacts.

## Phase 29 Outputs

| Area | Output |
| --- | --- |
| Core | `mcp/samvil_mcp/evolve_cycle.py` |
| Artifacts | `.samvil/evolve-cycle.json` |
| MCP | `build_evolve_cycle_closure`, `materialize_evolve_cycle_closure` |
| Runtime | run-report and `samvil-status` evolve cycle summaries |
| Dogfood | `scripts/phase29-evolve-cycle-closure-dogfood.py` |
| Tests | evolve cycle contract, status, telemetry, MCP smoke, Phase 29 dogfood |

## Work Plan

### W1. Closure Contract

- [x] Read current seed, post-rebuild QA request, and latest QA results.
- [x] Verify latest QA result is newer than the pre-rebuild blocked QA result.
- [x] Map QA PASS to `closed`.
- [x] Map QA REVISE to `continue_evolve`.
- [x] Map QA FAIL or failed convergence to `failed`.
- [x] Block closure when seed hash/version or QA iteration is inconsistent.

### W2. Surfaces

- [x] Add run-report `evolve_cycle` summary.
- [x] Add `samvil-status` human/JSON output.
- [x] Add MCP tools for build/materialize.

### W3. Verification

- [x] Add Phase 29 dogfood scenario.
- [x] Add Phase 29 pytest coverage.
- [x] Run direct Phase 29 dogfood.
- [x] Run evolve cycle pytest.
- [x] Run default release check runner.
- [x] Build release evidence bundle.
- [x] Run full pre-commit.
- [ ] Use verified publisher to push main, wait for CI, verify artifact gate,
  and push `v3.31.0`.

## Acceptance

- A successful post-rebuild QA pass closes the evolve cycle.
- A still-revising post-rebuild QA result routes back to `samvil-evolve`.
- Closure blocks if the QA result is stale.
- Release runner includes Phase 29 before Phase 28.
