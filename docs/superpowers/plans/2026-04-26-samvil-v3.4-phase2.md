# SAMVIL v3.4 Phase 2 — Multi-Host Skill Migration + Pattern Registry

**Date:** 2026-04-26
**Base:** v3.3.1 4-Layer Portability Foundation
**Target:** v3.4.0

## Goal

Turn the v3.3 skeleton into a practical multi-host SAMVIL runtime:

1. migrate the remaining high-traffic skills to ultra-thin MCP-driven bodies
2. add real Codex/OpenCode continuation adapters
3. enrich Codebase Manifest with pattern/dependency intelligence
4. add a regression suite that proves the same seed can progress across hosts

## Phase 2 Outputs

| Area | Output |
|---|---|
| Skill migration | `samvil-interview`, `samvil-design`, `samvil-build`, `samvil-qa` ultra-thin PoCs |
| Host adapters | Codex/OpenCode file-marker resume recipes and smoke scripts |
| Pattern Registry | `mcp/samvil_mcp/pattern_registry.py` + references schema |
| Manifest v2 | static import dependencies + module summaries |
| Regression suite | cross-host replay fixtures under `mcp/tests/fixtures/phase2/` |

## Workstreams

### W1 — Skill Migration Harness

- [x] Define a repeatable migration checklist from `samvil-seed`.
- [x] Create `scripts/skill-thinness-report.py` to track active vs legacy line counts.
- [x] Migrate `samvil-design` first because it benefits most from Orchestrator.
- [x] Acceptance: active design skill under 120 lines, legacy preserved, skill wiring PASS.

W1 result:
- `references/skill-migration-checklist.md` defines the repeatable migration
  shape.
- `scripts/skill-thinness-report.py --migrated-only --fail-over 120` guards
  active migrated skills.
- `skills/samvil-design/SKILL.md` is now 118 lines and
  `skills/samvil-design/SKILL.legacy.md` preserves the 649-line legacy body.
- `scripts/check-skill-wiring.py` now checks the migrated design skill's
  orchestration tokens.

### W2 — Codex/OpenCode Continuation

- [x] Standardize `.samvil/next-skill.json` shape.
- [x] Add `references/host-continuation.md`.
- [x] Add smoke script that reads a marker and prints the next action for Codex CLI.
- [x] Acceptance: file-marker chain from seed -> design and seed -> council is reproducible without Claude Skill tool.

W2 result:
- `references/host-continuation.md` defines marker schema `1.0`.
- `scripts/host-continuation-smoke.py` validates required fields and prints
  the next Codex/OpenCode action.
- `skills/samvil-seed/SKILL.md`, `skills/samvil-design/SKILL.md`, and
  `references/host-capability-schema.md` now use the canonical marker shape.
- Smoke verified both `seed -> samvil-design` and `seed -> samvil-council`
  markers without Claude Skill tool.

### W3 — Pattern Registry

- [x] Add registry of framework/domain patterns: Next.js app-router, Vite, Phaser, Expo, dashboard.
- [x] Expose MCP read/render tools.
- [x] Connect Build/QA prompts to pattern IDs, not copied paragraphs.
- [x] Acceptance: registry lookup works without scanning long skill bodies.

W3 result:
- `mcp/samvil_mcp/pattern_registry.py` defines five built-in patterns:
  Next.js app router, Vite React, Phaser game, Expo mobile, Recharts dashboard.
- MCP tools added: `list_patterns`, `read_pattern`, `render_pattern_context`.
- `skills/samvil-build/SKILL.md` and `skills/samvil-qa/SKILL.md` now request
  rendered pattern context by `solution_type` and framework.
- `references/pattern-registry-schema.md` documents the schema and tool set.

### W4 — Manifest v2 Intelligence

- [x] Add import graph extraction for TypeScript/JavaScript/Python.
- [x] Add module summaries with explicit `generated_by` and timestamp metadata.
- [x] Add confidence tags for regex/AST/manual sources.
- [x] Acceptance: real Next.js smoke shows dependencies among at least 3 modules.

W4 result:
- Manifest schema bumped to `1.1`.
- `mcp/samvil_mcp/manifest.py` now infers internal dependencies from TS/JS
  static imports, re-exports, `require()`, dynamic imports, Python imports,
  relative paths, `@/`, `src/`, and bare module-root imports.
- Each module gets deterministic heuristic summary metadata:
  `summary_generated_by`, `summary_generated_at`, and confidence tags such as
  `imports:regex` and `summary:heuristic`.
- Tests include a Next-style smoke where `app -> auth/features` and
  `features -> auth/ui`.

### W5 — Cross-Host Regression Suite

- [x] Capture one small web-app seed fixture.
- [x] Replay interview-summary -> seed -> design handoff across Claude-style and file-marker style hosts.
- [x] Add pre-commit smoke for host adapter regressions.
- [x] Acceptance: same seed reaches the same next-stage marker under both host modes.

W5 result:
- Fixture added under `mcp/tests/fixtures/phase2/small-web-app/`.
- `scripts/phase2-cross-host-smoke.py` replays the same seed through
  `claude_code` (`skill_tool`) and `codex_cli` (`file_marker`) continuation.
- Both hosts normalize to `seed_next=samvil-design` and
  `design_next=samvil-scaffold`.
- `scripts/pre-commit-check.sh` now includes the cross-host replay smoke.

## Non-Goals

- Full replacement of all 15 skills in one release.
- Real remote Codex/OpenCode process orchestration.
- Semantic code search beyond explicit manifest/pattern fields.
- Breaking seed schema changes.

## Release Bar

- [x] Full test suite green.
- [x] Pre-commit check green.
- [x] At least two active skills are ultra-thin and host-aware.
- [x] Codex-style file-marker continuation documented and smoke-tested.
- [x] v3.4.0 changelog records which skills remain legacy.

Release result:
- Version target: `v3.4.0`.
- Active ultra-thin skills: `samvil-seed`, `samvil-design`.
- Final verified baseline: 773 tests, 124 MCP tools, pre-commit PASS.
