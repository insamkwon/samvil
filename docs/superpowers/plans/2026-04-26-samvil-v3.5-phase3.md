# SAMVIL v3.5 Phase 3 — Telemetry + Run Observability

**Date:** 2026-04-26
**Base:** v3.4.0 Multi-Host Runtime + Pattern Registry
**Target:** v3.5.0

## Goal

Turn SAMVIL's existing files and MCP traces into an operator-readable run
telemetry layer. Phase 3 should answer: what happened, where did it slow down,
what failed, what is blocked, and what should a future retro learn?

## Phase 3 Outputs

| Area | Output |
|---|---|
| Run report | `.samvil/run-report.json` + MCP build/read/render tools |
| Event taxonomy | normalized stage event categories and failure/retry counters |
| Health rollup | MCP health failures grouped by tool and recent failure |
| Retro feed | telemetry observations promoted into retro candidates |
| Status surface | `samvil-status` / report rendering shows one-screen run state |

## Workstreams

### W1 — Run Telemetry Snapshot

- [x] Add `mcp/samvil_mcp/telemetry.py`.
- [x] Build report from `project.state.json`, `.samvil/claims.jsonl`,
  `.samvil/events.jsonl`, `.samvil/mcp-health.jsonl`, and
  `.samvil/next-skill.json`.
- [x] Expose MCP tools: `build_run_report`, `read_run_report`,
  `render_run_report`.
- [x] Acceptance: fixture report shows stage, gate counts, health failures,
  continuation marker, and next action.

W1 result:
- `mcp/samvil_mcp/telemetry.py` builds deterministic run reports without LLM
  calls.
- MCP tools added: `build_run_report`, `read_run_report`, `render_run_report`.
- `references/run-report-schema.md` documents report shape and tool contract.
- Tests cover file summary, roundtrip persistence, markdown rendering, MCP
  wrappers, missing report, and invalid project root.

### W2 — Event Taxonomy + Retry Counters

- [x] Categorize stage events into start, complete, fail, retry, blocked, skip.
- [x] Add per-stage durations when timestamps exist.
- [x] Track retry/fix loops and failure signatures.
- [x] Acceptance: a run with build fail + fix + pass renders a coherent timeline.

W2 result:
- Run reports now include `timeline.category_counts`, per-stage status,
  duration, event counts, failure count, and retry count.
- Event categories cover start/complete/fail/retry/blocked/skip/other.
- Tests cover build fail + fix + completion and QA blocked timelines.

### W3 — Retro Observation Feed

- [x] Convert report findings into retro candidate observations.
- [x] Deduplicate recurring failures by tool/stage/signature.
- [x] Add MCP tool or script to append selected observations.
- [x] Acceptance: telemetry can propose at least one actionable retro row.

W3 result:
- Run reports now group MCP failure signatures by tool/error for retro reuse.
- `derive_retro_observations` proposes deterministic candidates from failed or
  blocked stages, retry loops, MCP failures, and pending claims.
- `append_retro_observations` writes selected rows to
  `.samvil/retro-observations.jsonl` while skipping existing dedupe keys.

### W4 — Status Surface Integration

- [ ] Teach `scripts/samvil-status.py` to read `.samvil/run-report.json`.
- [ ] Add a concise human report for non-engineer status inspection.
- [ ] Acceptance: one command shows stage, gates, health, continuation, and next action.

### W5 — Phase 3 Dogfood + Release

- [ ] Run telemetry snapshot against synthetic and live-ish project fixtures.
- [ ] Record discovered blind spots.
- [ ] Release as `v3.5.0`.

## Non-Goals

- External analytics service.
- LLM-based telemetry summaries in W1.
- Replacing Claim Ledger or EventStore.
- Real-time dashboard UI.

## Release Bar

- Full test suite green.
- Pre-commit check green.
- Run report tools are deterministic and file-only.
- Status surface can explain the latest run without reading raw JSONL.
